#include <iostream>
#include <cpu.hpp>
#include <Memory.hpp>

int main()
{

    // Memory memory;

    // memory.write(0x00,0x46);

    // CPU cpu(memory);

    // uint8_t opcode = cpu.fetch();

    // cpu.decode(opcode);

    std::cout << "Frontend" << std::endl;
}